## Barkoder NativeScript App

NativeScript port of `C:\dev\BarkoderApp`, preserving the same screens, Barkoder scanning modes, history flow, details view, gallery scan, and about screen.

### Commands

```bash
npm install
npm run typecheck
npm run build:android
```

`build:ios` is also configured, but iOS builds require macOS.
